"""
2단계: 데이터 정제 및 변환 연습문제
"""

# 문제 1: 이상치 탐지 및 처리
# sales_data.csv에서:
# - IQR 방법으로 'amount' 컬럼의 이상치 탐지
# - Z-score 방법으로 이상치 탐지
# - 이상치를 median 값으로 대체
# - 처리 전후 비교 시각화

# 문제 2: 데이터 정규화
# customer_metrics.csv에서:
# - Min-Max 정규화 적용
# - Standard 정규화 적용
# - Robust 정규화 적용
# - 각 방법의 결과 비교

# 문제 3: 피처 엔지니어링
# user_activity.csv에서:
# - 날짜에서 요일, 월, 분기 추출
# - 로그인 시간으로부터 시간대 구분 (아침/점심/저녁/밤)
# - 연속된 로그인 일수 계산
# - 활동 점수 생성 (로그인 횟수 + 페이지뷰 * 0.1 + 구매 * 10)

# 문제 4: 데이터 검증 규칙 구현
# products.csv에서:
# - 가격이 0보다 큰지 검증
# - 이메일 형식이 올바른지 검증
# - 날짜가 유효한 범위인지 검증
# - 필수 필드가 누락되지 않았는지 검증
# - 검증 실패한 레코드 리포트 생성

# 문제 5: 텍스트 데이터 전처리
# reviews.csv에서:
# - 텍스트 소문자 변환
# - 특수문자 제거
# - 불용어 제거
# - 텍스트 길이 계산
# - 감정 점수 계산 (간단한 키워드 기반)